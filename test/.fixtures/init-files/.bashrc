# Test initialization file for testing typex -i / typex -u.

export PATH+=":$HOME/bin"

HISTCONTROL='ignoredups' # Do not store (adjacent) duplicates in the history; note: no need to export this variable; strangely, it doesn't seem to be effective when inherited.
export EDITOR='sublwait'
